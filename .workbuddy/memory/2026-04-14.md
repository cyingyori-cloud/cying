# 2026-04-14 工作日志

## MCP 双向联动闭环跑通 ✅

### 实测流程（完整成功）

1. **PowerQuote MCP 计算匹配** → 调用 `calculate_demand_matching`
   - 输入：420kW / 60kWh / 15min / DC 520-680V
   - 输出：demandId `demand_1776130252176`，50个方案，17个有效
   - 最优：PAI-200kW AIDC 模块化 UPS (P001-M8-D)

2. **查询 CRM 商机** → `NewOpportunityObj`
   - 海外EPC工程数字化管控平台采购，ID: `69c3be786967160008cda8ae`
   - 客户：许继集团有限公司，ID: `660cd7068b12db0001e00714`

3. **写回 CRM** → `UpdateRecordsByData` 更新产品需求申请
   - 对象：`product_requirement_applic__c`，ID: `69da18029859ed000739cb8a`
   - 字段：`special_requirement_remark__c`（特殊需求/备注）
   - 结果：writeDB: true ✅

### 候选方案清单写入成功
- 对象 `candidate_solution_list__c`（候选方案清单）可 Create
- 主表关联字段：`product_requirement_applic__c` → 产品需求申请 ID
- 关键可写字段：module_count__c / cabinet_number__c / ai_matching_analysis_statu__c / min/max_vdc__c / max_discharge_current__c / backup_power_duration_eol__c
- AI状态枚举：1=推荐方案, 2=电流边界, 3=时长临界, 4=可直接推进, other=其他
- 实测写入 5 条，全部 writeDB: true ✅
- ⚠️ 注意：candidate_solution_list__c 是明细行子表，Create 操作成功；QuoteObj 的 Create 在沙箱受限

### 自动化同步方案（轮询模式）
- **按钮** → 更新 `sync_status__c` = "待同步"（防重复提交）
- **自动化任务** `powerquote-crm` → 每小时执行一次，检测待同步记录并处理
- 新增字段 `sync_status__c`：选项为 待同步/同步中/已完成/同步失败
- 新增字段 `estimated_voltage_v__c`（估算电压 V）：存 PowerQuote 返回的 plan.estimatedVoltage
- 按钮代码：`docs/fxiaoke-button-sync-v1.groovy`，按钮 API Name: `button_view_sku_plan_list__c`
- 自动化 prompt 已配置完整闭环逻辑

### PowerQuote 返回字段 → CRM 字段映射
- plan.moduleCount → module_count__c
- plan.cabinetCount → cabinet_number__c
- plan.estimatedVoltage → estimated_voltage_v__c（CRM新增字段）
- plan.estimatedCurrent → max_discharge_current__c
- plan.estimatedBackupMinutes → backup_power_duration_eol__c
- plan.analysisStatusLabel → ai_matching_analysis_statu__c 映射：推荐方案=1, 电流超界/超限需复核=2, 时长临界=3, 可直接推进=4, 其他=other
- dc_min/max_voltage__c（输入参数）→ min/max_vdc__c
- ⚠️ min/max_vdc__c 所有方案相同，estimatedVoltage 每个方案不同才是真正的计算结果
